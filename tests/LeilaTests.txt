Testovi za projekat:

	main = function function 4;
	function x = 6;     -- runtime error: cannot execute return

	main = 3 - - (-4); -- rezultat je -1

	main = function 5;
	function x = function2 x;
	function2 x = function3 x;
	function3 x = "majmunee";  -- lexical error: invalid input '"'

	main = function 5;
	function x = function2 x;
	function2 x = function3 x;
	function3 x = 0; -- rezultat je 0

	main = putStr "majmunee"; --lexical error invalid input '"'

	main = - (- (- (- (- 4)))); -- rezultat je -4

	main = 34 + x;
	x = y;
	y = x;  -- expected token ;, found end of input

	main = let main in main;
	main = 8; --expected local definition, but found token "main"

	main = majmunee 5;
	majmunee x = if x == 0 then 1*x else 2; -- rezultat je 2

